// - We can assign variable to functions that return by using the keyword var (can store all variable types).
var username = prompt();
alert(username);
console.log(username);

// - We assign variable to make it easier for us to alter data instead of changing each single "age" we can just change the value of the var age.
var age = 5;
console.log(age);
console.log(age);
console.log(age);

//Single Line-Comment

/*
    Multi-line
    Comment
*/
