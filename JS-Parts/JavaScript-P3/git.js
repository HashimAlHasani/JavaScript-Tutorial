// - We declare a variable and then assign a value to it, also we can make expressions such as message variable below.
var age = 50;
var newAge = age;

var message = "newAge = " + newAge; // "newAGe = 50"
console.log(message);

//declaration
//initialization

//To check the console.log(message) open the index.html, right click, press inspect then open the console
//Refresh the page if you didn't see the value (unlikely to happen)